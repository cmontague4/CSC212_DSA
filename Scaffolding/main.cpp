#include <iostream>
#include <string>
#include <fstream>

int main(int argc, char* argv[]) {


    // This main will be included in each of the 3 shape files, but the
    // CLAs and body will vary slightly depending on which shape it's being used for


    // std::string out_string = shape_function(shape function inputs)
    //std::ofstream out_file(argv[x]);

    /*
    for (int i = 0 ; i < out_string.size() ; i++) {
        out_file << out_string[i] << " ";
    }
    */


}
